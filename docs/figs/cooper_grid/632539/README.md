> # Bounds rule not applicable
>
> `permev F` — permeability is fixed, so `kpmax`/`kpmin` are never used.

# Run 632539

**Verdict: NOT APPLICABLE** — permev F — permeability is fixed, so kpmax/kpmin are never used and the rule does not apply

What it was: Storage sweep on Taiyi's permeability map, understressed fault.

This folder predates the grid search and is **not** one of the 68 scored runs. See [`../BOUNDS_AUDIT.md`](../BOUNDS_AUDIT.md) for the full audit and [`../RUN_KEY.md`](../RUN_KEY.md) for the runs that are scored.

This run **is** cited elsewhere in the repo, so it is provenance for current work: `docs/cooper_basin/progress_report.py`
